# Review Conventions

Settled style/maintainability decisions for reviewing this repo. Edit this
file directly, or ask Claude to add an entry (e.g. "stop flagging X" or run
`/pr-review-copilot:team-conventions`). Commit it so the whole team reviews
against the same rules.

Note: entries here only suppress style/maintainability findings. Security
and correctness findings are never suppressed by a convention entry.

## General

- (no entries yet)

## TypeScript

- (no entries yet -- e.g. "`any` is allowed in `*.test.ts` files")

## Python

- (no entries yet)

## Tests

- (no entries yet -- e.g. "hotfix branches are not required to add new test coverage")
